import streamlit as st
from utils.ui import inject_custom_css, sidebar_branding

# Inject UI components
inject_custom_css()
sidebar_branding()

st.markdown("<div style='padding: 48px 0'>", unsafe_allow_html=True)
st.info("EDA DASHBOARD: SYSTEM OFFLINE. Requires live database connection.")
